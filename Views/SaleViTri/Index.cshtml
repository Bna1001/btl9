@model IEnumerable<btlnhom09.Models.SaleViTri>

@{
    ViewData["Title"] = "Index";
}

<h1>Phòng Kinh Doanh</h1>

<p>
    <a asp-action="Create">Thêm mới 1 vị trí</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th hidden>
                @Html.DisplayNameFor(model => model.VitriSale)
            </th>
            <th>Vị Trí</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.VitriSale)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ViTriSaleID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ViTriSaleID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ViTriSaleID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
